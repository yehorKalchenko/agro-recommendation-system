[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: Integration tests that hit full pipeline
    unit: Unit tests for isolated components
    slow: Tests that take significant time
filterwarnings =
    ignore::DeprecationWarning
